shared_config:
  - broker_connection: &broker_connection
      dev_mode: ${SOLACE_DEV_MODE, false}
      broker_url: ${SOLACE_BROKER_URL, ws://localhost:8008}
      broker_username: ${SOLACE_BROKER_USERNAME, default}
      broker_password: ${SOLACE_BROKER_PASSWORD, default}
      broker_vpn: ${SOLACE_BROKER_VPN, default}
      temporary_queue: ${USE_TEMPORARY_QUEUES, true}

  - models:
    # Claude 4.5 Sonnet via LiteLLM proxy (OpenAI-compatible)
    claude_sonnet: &claude_sonnet_model
      model: ${LITELLM_MODEL, openai/bedrock-claude-4-5-sonnet}
      api_key: ${LITELLM_API_KEY}
      api_base: ${LITELLM_API_BASE, https://lite-llm.mymaas.net}
      cache_strategy: "5m"
      parallel_tool_calls: true

    # Alias for backward compatibility
    planning: &planning_model
      <<: *claude_sonnet_model

    general: &general_model
      <<: *claude_sonnet_model

    image_gen: &image_generation_model
      model: ${IMAGE_MODEL_NAME, dall-e-3}
      api_base: ${IMAGE_SERVICE_ENDPOINT}
      api_key: ${IMAGE_SERVICE_API_KEY}

    report_gen: &report_generation_model
      <<: *claude_sonnet_model

    multimodal: &multimodal_model
      <<: *claude_sonnet_model

  - services:
    # Default session service configuration
    session_service: &default_session_service
      type: "memory"
      default_behavior: "PERSISTENT"
    
    # Default artifact service configuration
    artifact_service: &default_artifact_service
      type: "filesystem"
      base_path: "/tmp/samv2"
      artifact_scope: namespace
    
    # Default data tools configuration
    data_tools_config: &default_data_tools_config
      sqlite_memory_threshold_mb: 100
      max_result_preview_rows: 50
      max_result_preview_bytes: 4096